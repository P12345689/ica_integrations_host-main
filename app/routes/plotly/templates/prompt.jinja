Please interpret the following request for a chart and provide the necessary data to generate it using Plotly:

{{ query }}

Respond with a JSON object that includes the following fields:
- chart_type: The type of chart to generate (e.g., 'bar', 'pie', 'line', 'scatter')
- data: A dictionary containing the data for the chart (e.g., {"x": [...], "y": [...]} for bar and line charts, or {"labels": [...], "values": [...]} for pie charts)
- title: A title for the chart

Your response should be a valid JSON object only, without any additional text or explanation. Do not include backticks or any formatting. The JSON object should be directly parseable by Python's json.loads() function. Ensure all keys and string values are enclosed in double quotes, not single quotes.

Example of a valid response:
{"chart_type": "pie", "data": {"labels": ["A", "B", "C"], "values": [30, 40, 30]}, "title": "Sample Pie Chart"}
